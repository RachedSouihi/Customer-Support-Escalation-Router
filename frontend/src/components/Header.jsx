import "./Header.css";

function Header() {
  return (
    <header className="header">
      <h1>Customer Support Escalation Router</h1>
      <p>
        An agentic workflow that classifies tickets, assigns priority, routes
        them, and drafts responses in one clear flow.
      </p>
    </header>
  );
}

export default Header;